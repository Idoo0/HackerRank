#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    
  int n,k,m=0,res=0;
  cin >> n >> k;


  vector<int> diff;
  for(int i=1; i<=sqrt(n); i++){
    
    if(n % i == 0){
      if(find)
    }
    
  }
  return 0;
}
